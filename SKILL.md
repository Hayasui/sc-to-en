---
name: sc-to-en
description: 把用研 Markdown 文档（访谈大纲、玩家甄别问卷、测试脚本）从简体中文定稿本地化成可直接配置到 UserTesting 的英文版，也用于中文改完后同步英文版。当用户提供中文用研 md 要求"翻译成英文版""本地化""转成英文""配置到 UserTesting""英文版更新""中文改了同步英文"时使用；也用于新建用研 md 时提供文档骨架与结构规范。适用于游戏/产品用研场景下基于 UserTesting（Unmoderated Test）的测试材料。
agent_created: true
---

# 用研文档 · 中文定稿 → 英文本地化 → UserTesting 配置

把简体中文（SC）的用研 Markdown 文档，本地化成**可以直接逐条复制到 UserTesting 平台**的英文版。
不是直译——是让美国普通玩家读起来自然、同时满足平台的所有字段限制。

## 何时使用

用户的固定工作流：

> **用 md 写/改中文定稿 → 本地化成英文版（不直译）→ 拿着英文版在 UserTesting 上配置**

出现以下情况时使用本 skill：

- 用户给出中文用研 md（访谈大纲 / 甄别问卷 / 测试脚本），要求产出或更新英文版
- **中文版改过了，要同步到英文版**（增量修订，见第 0 步场景 C）
- 用户提到「本地化」「不要直译」「英文版」「配置到 UserTesting」「英文问卷」
- 用户要**新建**用研 md，需要文档骨架与结构规范

**不适用**：把 md 转成 Excel 留档——那是 `md-to-research-xlsx` 的职责（见文末衔接）。

## 核心原则

1. **版本链条**：**中文 md 是源头**（内容设计在此定稿，也是内部审阅、与需求方沟通的基准）→ **英文 md 是本地化产物**（面向玩家，措辞可自由）→ xlsx 是下游渲染。英文版可以为了本地化调整措辞，但**一旦动了语义，必须回写中文版**；纯措辞优化可单边改。防止两边漂移。
2. **玩家可见 vs 内部参考，严格分离**：文档里同时存在「玩家在平台上看到的内容」和「给内部看的研究参考」。**只把玩家可见部分当作语言对象**；内部备注、研究落点、附录 RQ 不追求"地道的玩家语言"。
   **但"不润色"不等于"不译"**：有人版整份是给英文主持人用的（提问话术要念、追问方向要看、观察记录点要记），内部栏目照译，
   只是保持精确、不做面向玩家的口语化改写。无人版里纯给研究团队看的分析附录按项目惯例办——
   `3D二合` 的现状是整份都译，沿用现状，不要在同步时把已译的部分退回中文。
3. **平台限制优先于语言美感**：一句话再漂亮，如果违反平台字段限制（字符数、选项标签、无法条件化等），也得改。先满足平台，再谈地道。
4. **面向普通玩家**：读者不是专业从业者。清晰、口语、无术语、无俚语。绝不出现「核心玩家 vs 破圈大众」「心流」「认知负担」这类词。

## 工作流程

### 第 0 步：判断场景

先判断是哪一种，走法不同：

| 场景 | 特征 | 怎么走 |
|------|------|--------|
| **A. 首次本地化** | 只有中文 md，还没有英文版 | 第 1–5 步完整走一遍 |
| **B. 新建文档** | 要起一份新问卷/大纲 | 读 `references/doc-structure.md` 的骨架生成框架，**不代写研究内容** → 之后按 A 走 |
| **C. 增量修订**（高频） | 中英版都已存在，中文版改过了，要同步到英文版 | 见下方「增量修订流程」 |

#### 增量修订流程（场景 C）

用户的工作习惯是：**中文改动先落，再同步英文**。所以修订时：

1. **只改被改动的地方**，不要重写整份文档。先定位中文版这次改了什么（问用户，或对比最近版本）。
2. **逐条同步到英文版对应位置**，保持题号/位置一一对应。
3. **判断改动性质**（这决定英文侧怎么改）：
   - **纯内容改动**（加了一道题、改了选项、改了题干意思）→ 英文版对应位置照改，**并做本地化**（不是直译）
   - **纯措辞改动**（中文换了种说法，意思没变）→ 英文版视情况跟着调整，也可以不动（英文本来就该自然，不必跟中文措辞）
   - **结构性改动**（删题、挪题、改题型、改流程）→ **先 grill 对齐**（见下文），再动手
4. **反向检查**：改完英文版后，回头确认中文版没有因为英文的本地化调整而需要再改（比如英文为了地道加了一句解释，中文要不要加）。
5. **跑 lint**（第 4 步），确认题号连续、中英对齐。
6. **汇报**：只报这次改了什么，不用重复整份文档。

> **关键**：修订时最容易出的事是"改了英文、忘了中文"或反过来。lint 的 `--sc` 中英对齐检查就是兜这个底的。

> **批量替换前必须先备份**（2026-09-09 教训）：用脚本批量改题号/术语时，
> 先把文件复制一份（或确认已纳入版本控制），再小样验证替换结果，最后全量执行。
> 那次正则捕获组写错，120 处题号被吞成控制字符，文件无版本控制、无法回滚，
> 只能逐处人工还原。版本控制见「维护」一节。

### 第 1 步：读文档，分清两类内容

通读中文 md，在心里（或列表）把内容分成两层：

| 层 | 内容 | 处理方式 |
|----|------|---------|
| **玩家可见** | Instruction text、Navigation task instructions、Question Page 标题、页面引导语、题干（Stem）、选项、Scale Labels | **重点本地化** |
| **内部参考** | 研究落点、内部备注、研究假设、附录 RQ、主持人提醒 | **保持原样**，不参与润色 |

这一步最容易出错：把内部备注也翻得花里胡哨，反而丢掉了研究团队需要的精确表述。

### 第 2 步：对照平台限制逐项检查

读 `references/platform-constraints.md`，把文档里**违反或可能违反**平台限制的地方找出来。常见问题：

- Instruction text / Task instructions 超 1000 字符
- 选项里写了 `please specify` / `请说明` / `请填写名字`（平台只显示 `Other` / `None of the above`）
- 需要玩家自填的选项，被设计成"跟随前一题答案动态生成"（平台做不到）
- 量表题缺 `Scale Labels`
- 英文版残留汉字 / 中文标点

发现问题先按限制改，再进入本地化。

### 第 3 步：本地化（分批交付）

读 `references/localization-principles.md`，逐句本地化**玩家可见部分**。
遇到研究概念、游戏术语时，先查 `references/glossary.md` 的固定译法，**不要临场另起一个说法**。

**分批交付是默认工作方式**（用户 Q10 定案）：

- 先做 **Task A（Instruction + Navigation）** → 交给用户确认方向
- 方向 OK 后，再按 **Question Page 逐页**推进
- 每批交付后等用户反馈，**不要一次性全改完**

理由：英文润色是主观活，方向不对全改完等于白做。用户会在 UserTesting 上分批配置、分批确认。

### 第 4 步：跑 lint 校验（**不可跳过**）

```bash
python scripts/lint_usertesting.py <英文版.md>
```

脚本会检查：残留汉字/中文标点、**隐形字符（控制字符 / 零宽字符 / BOM / 替换字符）**、
1000 字符超限、平台禁止的自填提示、量表题缺 Scale Labels、
**题号连续性与重复**、**题号交叉引用是否指向存在的题**、
**Task/Page 或环节结构完整性**。

```bash
python scripts/lint_usertesting.py <英文版.md> --sc <中文版.md>
```

带 `--sc` 时额外做中英题号/选项数对齐、中文版结构检查。

**必须修复报出的所有问题。改完脚本后跑一次自测：**

```bash
python scripts/test_lint.py
```

> **为什么不可跳过**（2026-09-09 教训）：那次改完中文版没跑 lint，
> 英文版落后一题、题号整体错位，隔了一轮才发现。lint 是唯一能自动发现
> "中英对不上"的环节，省掉它等于把核对成本转给人工，而人工一定会漏。

### 第 5 步：汇报

按**三段式**汇报（用户 Q14 定案）：

1. **改动清单**：改了哪些位置
2. **理由**：每处为什么改（尤其是"中式英文 → 地道表达"的地方，用户要能判断是否接受）
3. **验证结果**：lint 通过情况、中英结构对齐情况

## 什么时候先 grill 再动手

用户 Q11 定案：**结构性改动先对齐，纯措辞直接动手**。

- **需要先 grill**：删题 / 加题、改题型、改流程（如 Q10-13 从甄别挪到试玩后）、平台限制导致的方案取舍、是否砍掉某个研究问题
- **直接动手**：措辞润色、语序调整、语法修正、加 Scale Labels 等不改变研究设计的工作

结构性改动不可逆、影响面大，值得先讨论清楚；措辞层面的先 grill 反而拖节奏。

## references 导航

| 文件 | 内容 |
|------|------|
| `references/platform-constraints.md` | UserTesting 平台限制与坑（字符数、选项标签、条件逻辑的真实能力与取舍、Scale Labels、分页、NPS 例外、加粗失效等） |
| `references/localization-principles.md` | 英文本地化标准（不直译、不用俚语、Title Case、术语不译、面向普通玩家的措辞） |
| `references/doc-structure.md` | md 文档结构规范（玩家可见/内部参考分离、每题字段、可直接粘贴的状态、新建骨架） |
| `references/glossary.md` | **术语对照表**——中英固定说法，保证跨文档/跨会话一致 |
| `references/checklist.md` | 交付前逐项检查清单 |
| `references/roadmap.md` | 升级路线图、改进候选、怎么提升级需求 |

## 与 md-to-research-xlsx 的衔接

**版本链条**（三个环节的职责，别搞混）：

```
中文 md  ← 源头（内容设计在此定稿）
   ↓ 本地化（本 skill）
英文 md  ← 产物（面向玩家，措辞可自由）
   ↓ 渲染（md-to-research-xlsx）
xlsx     ← 留档（中文列照中文 md、英文列照英文 md，忠实呈现）
```

本 skill 是上游：产出/更新的中英文 md，若需要转成**中英对照 Excel 留档**（上传在线文档、与需求方沟通），
交给 `md-to-research-xlsx` 处理。

**何时才需要生成 xlsx**：xlsx 是**按需生成**的留档件，不是每次改 md 都要同步。
典型触发是**组内讨论、与需求方沟通**这类需要"拿一份表出去"的场合。
md 还在改的时候不用管 xlsx；等要用了再生成一次即可。

**xlsx 不参与版本判定**：它只是渲染产物，中英不一致时报出来由用户判断，
不会自行裁决（详见 `md-to-research-xlsx` 的 `format-spec.md`）。

## 维护

改这个 skill 时：先把这次的摩擦点写进 `references/` 对应文件，再动 SKILL.md 的流程。
提需求时**描述遇到的摩擦**（"英文版翻完，中文版没跟上，两边对不上了"），
不要指定实现方案——前者留出解法空间。

### 版本控制

skill 目录（`~/.workbuddy/skills/sc-to-en/`）与项目文档目录都应纳入 git：

- **skill 目录**管代码（`scripts/`）与规范（`references/`、`SKILL.md`）的迭代
- **项目目录**管研究文档（md）的迭代

两者改动性质不同，分开建仓，提交历史才读得懂。
纯文本、体积小，git 的代价几乎为零；而不上版本控制的代价已经在 2026-09-09
付过一次——批量替换事故无法回滚。

**改脚本的纪律**：改完 `lint_usertesting.py` 必须跑 `python scripts/test_lint.py`，
**全部用例通过**才算改完（2026-09-14 是 31 个，这个数字随用例增补而变，别把它当常量）。
用例是成对的（正例 + 反例），防的是"检查写太严误报"和"检查写太松漏报"两个方向。
